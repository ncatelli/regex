[package]
name = "tests"
version = "0.2.0"
license = "Apache-2.0"
edition = "2021"
workspace = ".."

[dependencies]
regex-runtime = { path = "../runtime/" }
regex-compiler = { path = "../compiler/" }
